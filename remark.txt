В общем по купюрам.


+ 1. poll_device - за что отвечает ? функция на 700 строк... которая практически только пишет лог в консоль.

+ 2. вынести из класса функции get_int_str, get_int_int_str, etc...  в utils где им и место

+ 4. провести в целом рефакторинг, pep8 хотябы, удалить не нужный закоменченный код

6. smartpayout_def.py - константы должны быть отдельно от функций и тем более
   что тут забыл конфиг для запуска скрипта из пунта 5






3. Разделить класс на несколько: Работа с serial, шифрование, для управления платежами, Events,
   и все это разумеется в конце привести к одному SmartPayout.
   Сейчас в нем сборная солянка в которой все подряд и где подряд





9. Timeout на операции приемки

10. из коробки это не должно быть как поток, все должно быть последовано и ясно.
    а уже при использование пакета мы сами обвернем в поток что требуется и будем с ним работать.





8. упаковать это все нормально в пакет - это и должен был быть пакет который
   мы подключаем и с кайфом работаем, а не кусок кода.



5. добавить полноценные examples для работы с библиотекой, подключение девайса, выдача/приемка,
   конфигурация etc  и выкинуть в конце файла smartpayout.py подобие утилиты для тестов, перенести,
   если уж ее и делать то работоспособной, а не менять в репозитории строку с портом для его запуска хотябы!

7. добавить readme с рассказом о его работе и что требуется для работы.







11. Хочется видеть raise с ошибками а не только логи с этими ошибками - нам нужно обработать ошибку, а не только получить True/False







По рефакторингу:
1. poll_device - за что отвечает ? функция на 700 строк... которая практически только пишет лог в консоль.
>>> мониторинг устройства, логирование событий

2. вынести из класса функции get_int_str, get_int_int_str, etc...  в utils где им и место
>>> ок

3. Разделить класс на несколько: Работа с serial, шифрование,  для управления платежами, Events, и все это разумеется в конце привести к одному SmartPayout. Сейчас в нем сборная солянка в которой все подряд и где подряд
>>> будет разделен на два класса Device (низкоуровневый для с командами устройства) и SmartPayout (обертка с потоком)

4. провести в целом рефакторинг, pep8 хотябы, удалить не нужный закоменченный код
>>> ок

5. добавить полноценные examples для работы с библиотекой, подключение девайса, выдача/приемка, конфигурация etc  и выкинуть в конце файла smartpayout.py подобие утилиты для тестов, перенести, если уж ее и делать то работоспособной, а не менять в репозитории строку с портом для его запуска хотябы!
>>> ок

6. smartpayout_def.py - константы должны быть отдельно от функций и тем более что тут забыл конфиг для запуска скрипта из пунта 5
>>> ок

7. добавить readme с рассказом о его работе и что требуется для работы.
>>> ок

8. упаковать это все нормально в пакет - это и должен был быть пакет который мы подключаем и с кайфом работаем, а не кусок кода.
>>> ок

9. Timeout на операции приемки
>>> п.3 и п.10, всеми Timeout-ами управляете сами

10. из коробки это не должно быть как поток, все должно быть последовано и ясно. а уже при использование пакета мы сами обвернем в поток что требуется и будем с ним работать.
>>> ок (п.3 будет класс Device)

11. Хочется видеть raise с ошибками а не только логи с этими ошибками - нам нужно обработать ошибку, а не только получить True/False
>>> это и так есть (класс SSPResponse)